def foo<T, Q>(a: T*, b: vector<tuple<str***, bool, complexNumber>>): void* {}

let a: int = 0;

struct B {
    constructor() {}
    destructor {}

}

struct A extends B {
    a: int
    method b(): void {
        a;
    }

    constructor() {}

    destructor {
        a;
    }
}

enum Hola {
    hola,
    adios
}