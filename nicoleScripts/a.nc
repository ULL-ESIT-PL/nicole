def foo<T, Q>(a: T*, b: vector<tuple<str***, bool, complexNumber>>): void* {}


foor<int>{}.metodo<int>().metodos();



let a: int = 0;

struct A {
    a: int
    method b(): void {
        a;
    }

    constructor() {}

    destructor {
        a;
    }
}

enum Hola {
    hola,
    adios
}